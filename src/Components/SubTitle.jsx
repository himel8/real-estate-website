import React from "react";

const SubTitle = ({ subtitle }) => {
  return (
    <h4 className="text-2xl text-[#000000] font-primary uppercase">
      {subtitle}
    </h4>
  );
};

export default SubTitle;
